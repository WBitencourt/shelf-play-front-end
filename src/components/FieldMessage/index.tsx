import { Error } from './Error';
import { Tip } from './Tip';

export const FieldMessage = {
  Error: {
    Root: Error.Root,
    Text: Error.Text,
  },
  Tip: {
    Root: Tip.Root,
    Text: Tip.Text,
  }
}